# Public vars (accessible in browser)
PUBLIC_API_URL=http://localhost:4000
PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Private vars (server-side only)
API_URL=http://localhost:4000

# ── Sentry Error Tracking ─────────────────────────────────────────────────────
# Obtain from: https://sentry.io → Settings → Projects → roost-portal → DSN
# Must be PUBLIC (exposed to browser). Leave empty to disable.
PUBLIC_SENTRY_DSN=
