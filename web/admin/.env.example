# Roost Admin Panel Environment

# URL of the Roost API (all backend services behind a single gateway)
ROOST_API_URL=http://localhost:4000

# Port for the admin panel dev server
PORT=5174

# ── Sentry Error Tracking ─────────────────────────────────────────────────────
# Obtain from: https://sentry.io → Settings → Projects → roost-admin → DSN
# Must be PUBLIC (exposed to browser). Leave empty to disable.
PUBLIC_SENTRY_DSN=
